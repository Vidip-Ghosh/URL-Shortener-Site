<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <title>Document</title>
</head>
<body>
    <div class="container mt-5">
        <h1>URL Shortening Site</h1>
        <form action="/shortURL" method="post" class="my-4 form-inline">
            <!-- <label for="fullUrl">URL</label> -->
            <input required type="url" name="fullUrl" id="fullUrl" placeholder="Shorten your link" class="formControl mr-2 col">
            <button class="btn btn-success" type="submit">Shorten URL</button>
        </form>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Full URL</th>
                    <th>Shorten URL</th>
                    <th>Clicks</th>
                </tr>
            </thead>
            <tbody>
                <% shorturl.forEach(shortUrl =>{ %>
                    <tr>
                        <td><a href="<%=shortUrl.full %>"><%= shortUrl.full %></a></td>
                        <td><a href="<%=shortUrl.short %>"><%= shortUrl.short %></a></td>
                        <td><a href="<%=shortUrl.clicks %>"><%= shortUrl.clicks %></a></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html>